# --- Terraform ---
# Local .terraform directories (Plugins, modules)
**/.terraform/*

# .tfstate files (CRITICAL: Never commit these if you have secrets)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files which are likely to contain sensitive data
# (We pass variables via CI/CD pipelines instead)
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore the plan output files
*.tfplan

# --- AWS ---
# config files for CLI tools (should not be in repo)
.aws/

# --- IDE / OS ---
# VS Code
.vscode/
!.vscode/extensions.json

# Mac
.DS_Store

# JetBrains / IntelliJ
.idea/

# --- Application Specific ---
# If you have node_modules or build artifacts in the same repo
node_modules/
dist/
build/